<UserControl x:Class="MediaVault.LinkHub.App.Views.ScratchpadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}" Margin="0,0,8,0">
            <ListBox ItemsSource="{Binding QuickNotes}"
                     SelectedItem="{Binding SelectedNote}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Contenido}" TextTrimming="CharacterEllipsis" MaxWidth="260" />
                                <TextBlock Text="{Binding FechaCreacion, StringFormat=dd/MM/yyyy HH:mm}"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           FontSize="11" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}" Margin="8,0,0,0">
            <StackPanel>
                <TextBlock Text="Editor de nota" Style="{StaticResource SectionTitleStyle}" />
                <TextBox Text="{Binding Contenido, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         Height="260"
                         VerticalScrollBarVisibility="Auto" />
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <Button Content="Guardar" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding SaveCommand}" />
                    <Button Content="Nueva" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding ClearFormCommand}" />
                    <Button Content="Eliminar" Style="{StaticResource DangerButtonStyle}" Command="{Binding DeleteCommand}" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
